export const metadata = {
  title: 'Sponsors — 4C',
  description: 'Our Sponsors: The organizations and brands that support 4C NMIMS.',
};

export default function SponsorsLayout({ children }) {
  return <>{children}</>;
}
